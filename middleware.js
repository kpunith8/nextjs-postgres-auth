// import { withAuth } from '@kinde-oss/kinde-auth-nextjs/middleware'

export default function middleware(request) {
  // return withAuth(request)
  // return request
}

// // Chose the pages where the auth can be used, this can be used to
// // opt out the main page from users to view it without login
// export const config = {
//   matcher: '/tasks',
// }
